cycle_num: 2
title: Parser Efficiency Optimization
branch: refactor/prod-cycle2
target_files: src/parsing.y
scope: Parser efficiency improvements and helper function consolidation
lines_total: 889
risk_level: 🟢 LOW-MEDIUM
estimated_time: 1-1.5 hours
target_reduction: 15-25
target_lines: 864-874

situation: |
  Cycle 2 complements Cycle 1 (Foundation) by optimizing parser efficiency. 
  Baseline after Cycle 1: parsing.y at 889 lines. Focus on function consolidation.

refactoring_opportunities: |
  1. Consolidate near-duplicate helper functions
     - Event handling: new_event(), append_event(), add_event()
     - IR building: ir_builder_new_file(), ir_builder_new_memory()
     - Stream markers: ir_stream_start(), ir_stream_end()
  2. Remove redundant conditional logic (event capacity, buffer growth)
  3. Inline single-use helpers if found

baseline_commands: |
  grep -c "^static.*(" src/parsing.y  # Count helper functions

green_phase: |
  1. Identify near-duplicate functions with similar signatures
  2. Find redundant conditional checks (capacity, buffer sizing)
  3. Locate single-use helper candidates for inlining

refactor_approach: |
  - Merge 2-3 duplicate event/IR helpers
  - Consolidate conditional logic patterns
  - Inline 1-2 single-use functions

specific_changes: |
  1. Merge ir_builder_new_file() and ir_builder_new_memory() into unified ir_builder_new()
  2. Consolidate stream marker functions into direct calls
  3. Simplify event capacity check patterns

hints: |
  Pattern: Function Pairs - If f1 and f2 differ only in 1 parameter, create f(param=default)
  Pattern: Wrapper Functions - If f() just calls g(fixed_value), inline at call sites
  Pattern: Conditional Duplication - Extract common logic, parameterize differences
