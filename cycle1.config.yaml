cycle_num: 1
title: Parser Foundation
branch: refactor/prod-cycle1
target_files: src/parsing.y
scope: Parser rule consolidation and cleanup
lines_total: 911
risk_level: 🟢 LOW-MEDIUM
estimated_time: 1-2 hours
target_reduction: 15-20
target_lines: 890-895

situation: |
  Original Cycle 1 analysis targeted foundation structures. The analysis phase files 
  have been refactored into the current architecture:
  - src/parsing.y - Main parser (foundation equivalent)  
  - src/scanning.l - Lexer (already refactored ✓ in Cycle 5)
  - src/composition.y - Composition layer  
  - src/serialization.y - Serialization layer

refactoring_opportunities: |
  1. Duplicate Rule Consolidation - Look for rules with identical patterns
  2. Unused Rules - Search for rules never referenced (dead code)
  3. Inline Simple Rules - Single-production rules can be inlined
  4. Comment Cleanup - Remove obsolete or misleading comments
  5. Action Reduction - Simplify repetitive action blocks

baseline_commands: |
  grep -c "^\s*[a-z_]*\s*:" src/parsing.y  # Count grammar rules

green_phase: |
  1. Examine parser structure: grep -n "^\s*[a-z_]*\s*:" src/parsing.y | head -40
  2. Identify refactoring targets (identical productions, single-use rules)
  3. Plan specific changes (consolidation, inlining, cleanup)

refactor_approach: |
  - Remove max 3-5 dead code sections
  - Consolidate 1-2 obvious duplicates  
  - Simplify 2-3 trivial rules
  - Clean up comments

specific_changes: |
  1. Find and inline single-use delegation rules
  2. Consolidate nearly-identical grammar productions
  3. Remove commented-out rule variants
  4. Simplify verbose action blocks where action == pass-through

hints: |
  Pattern 1: Delegation Rules - Inline and delete
  Pattern 2: Identical Productions - Combine into single rule
  Pattern 3: Dead Code - Delete commented rule blocks
  Pattern 4: Verbose Comments - Replace with concise inline comments
